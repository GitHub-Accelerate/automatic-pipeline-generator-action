name: Execute Pipeline Gen Action

on: [push, pull_request]

jobs:
  run-action:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Run pipeline-gen action
        uses: ./
